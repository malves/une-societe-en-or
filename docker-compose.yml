services:
  app:
    image: kleekr/societeenor:v1.0.0
    container_name: famille-en-or
    ports:
      - "3010:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    volumes:
      - famille-data:/app/data
    restart: unless-stopped

volumes:
  famille-data:
